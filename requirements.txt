# Ultra-minimal requirements for Streamlit Cloud deployment
# Only the most basic packages that are guaranteed to work
streamlit==1.34.0
requests

# All other packages removed to resolve dependency installation errors
# Features requiring additional packages will show appropriate error messages