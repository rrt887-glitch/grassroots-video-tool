# Core LLM and API packages
langchain_openai==0.1.1
langchain==0.1.20
langchain-community==0.0.38
tenacity==8.3.0
openai==1.16.2

# Streamlit and basic utilities
streamlit==1.34.0
pillow
requests
aiohttp
numpy==1.26.4

# Chinese LLM services
qianfan==0.3.15
dashscope

# Cloud TTS services (lightweight)
aliyun-python-sdk-core==2.15.1
tencentcloud-sdk-python-tts==3.0.1168

# Basic utilities
psutil
pyperclip

# Removed packages that may cause deployment issues:
# selenium==4.20.0 - requires browser drivers and system dependencies
# faster-whisper==1.0.3 - requires audio system libraries
# azure-cognitiveservices-speech==1.36.0 - large package with system dependencies
# pydub - audio processing, may need ffmpeg integration issues
# wxPython - GUI library, not needed in cloud
# pyaudio - system audio library dependencies
# torch - version conflicts and large size
# pybase16384 - compatibility issues
# sherpa-onnx - system-specific dependencies